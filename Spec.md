# 破晓系统 Spec

## 1. 现系统拆解与问题诊断

### 1.1 审计目标

本节只处理《破晓系统》v1.0 的基线审计，输出三类结果：

1. 识别当前已经具备程序化基础的量化规则。
2. 标记仍依赖人工主观判断、暂时无法直接回测的规则点。
3. 找出可能导致胜率失真、回测失真或执行走形的规则漏洞，作为后续优化优先级。

### 1.2 当前系统的可量化规则清单

下表仅保留可以直接落成字段、阈值、事件或状态机条件的规则，默认后续都应进入策略引擎。

| 模块 | 规则项 | 当前阈值/定义 | 可程序化程度 | 所需字段 |
| :--- | :--- | :--- | :--- | :--- |
| 系统铁律 | 单票止损 | 浮亏触及 -7% 立即卖出 | 高 | 持仓成本、最新价、实时收益率 |
| 系统铁律 | 账户回撤熔断 | 单日账户亏损 > -10% 时当日空仓，次日半仓 | 高 | 日初权益、实时权益、账户收益率 |
| 系统铁律 | 禁止补仓 | 亏损仓位不得加仓 | 高 | 持仓方向、持仓收益率、委托行为 |
| 系统铁律 | 持股周期 | 持股 1 天，最长 2 天 | 中 | 买入日期、持仓天数 |
| 情绪闸门 | 绿灯 | 涨停家数 > 60、最高板 >= 5、昨日连板晋级率 > 30%、无跌停潮 | 中 | 涨停池、连板高度、晋级率、跌停统计 |
| 情绪闸门 | 黄灯 | 涨停家数 40-60、最高板 >= 4、无跌停潮 | 中 | 涨停池、连板高度、跌停统计 |
| 情绪闸门 | 红灯 | 涨停家数 < 40，或出现跌停潮，或最高板核按钮 | 中 | 涨停池、跌停池、龙头分时/开盘表现 |
| 情绪闸门 | 仓位上限 | 绿灯 100%，黄灯 40%，红灯 0-10% | 高 | 市场状态、账户仓位 |
| 一进二排除 | 流通市值 | 20 亿 - 200 亿 | 高 | 流通市值 |
| 一进二排除 | 品种限制 | 非 ST、非上市 60 日内新股、非创业板 300 | 高 | 股票标签、上市日期、代码前缀 |
| 一进二排除 | 年线过滤 | 首板收盘价 > MA250 | 高 | 日线收盘价、MA250 |
| 一进二排除 | 股性激活 | 60 个交易日内至少 1 次涨停 | 高 | 历史涨停事件 |
| 一进二排除 | 午后偷鸡板过滤 | 拒绝 13:30 后直线拉升封板 | 中 | 分钟线、首次涨停时间、涨停前斜率 |
| 一进二排除 | 首板封单金额 | 封死瞬间封单金额 >= 2000 万元 | 中 | 涨停时盘口封单、委买量、涨停价 |
| 一进二评分 | 封板时间 | 9:30-10:00 / 10:00-10:30 / 10:30 后分档 | 高 | 首次封板时间 |
| 一进二评分 | 封单质量 | <5 万手 / 5-10 万手 / >10 万手，叠加开板次数 | 中 | 封单手数、开板次数 |
| 一进二评分 | 换手率 | <3% 或 >20%、3%-5% 或 15%-20%、5%-15% | 高 | 当日换手率 |
| 一进二评分 | 板块效应 | 同板块涨停数 1 / 2 / >=3 只 | 中 | 板块映射、板块涨停统计 |
| 一进二入场 | 竞价高开区间 | 3%-6% | 高 | 昨收、竞价价 |
| 一进二入场 | 竞价量比阈值 | 竞价量 / 昨日总成交量约 5%-10% | 中 | 竞价成交量、前一日成交量 |
| 一进二入场 | 开盘承接 | 9:30-9:35 不瀑布杀、守住开盘价 | 中 | 1 分钟线、开盘价、最低价 |
| 一进二入场 | 拉升速度 | 5 分钟内涨幅 >= 7% | 高 | 1 分钟线、5 分钟涨幅 |
| 一进二入场 | 换手确认 | 5 分钟换手 >= 前日全天换手的 30% | 中 | 实时换手、前一日换手 |
| 一进二执行 | 首次追涨仓位 | 涨幅 7%-8% 追 40% 仓位 | 高 | 涨幅、仓位 |
| 一进二执行 | 回踩补仓 | 回踩不破分时均线再加至 60% | 中 | 分时均线、最低价、仓位 |
| 一进二执行 | 回封打板 | 炸板后再次回封时打满至系统上限 | 中 | 炸板事件、回封事件、仓位上限 |
| 卖出规则 | 低开止损 | 低开超过 -3%，10 分钟内不翻红则离场 | 中 | 开盘涨跌幅、10 分钟最高价 |
| 卖出规则 | 破均线减仓 | 跌破分时均线 3 分钟不回，卖半仓 | 中 | 分时均线、持续时间、仓位 |
| 卖出规则 | 破开盘价清仓 | 跌破开盘价立即清仓 | 高 | 开盘价、最新价 |
| 卖出规则 | 尾盘强制卖 | 14:55 未涨停且非总龙头则清仓 | 中 | 时间、是否涨停、龙头标签 |
| 卖出规则 | 第三天强卖 | 非总龙头持有至第三天竞价卖出 | 高 | 持仓天数、龙头标签 |
| 弱转强识别 | 前一日弱板形态 | 烂板 / 14:30 后尾盘板 / 炸板回封，换手 5%-15% | 中 | 涨停时间、开板次数、换手率 |
| 弱转强识别 | 次日转强确认 | 高开 >= 5%，3-5 分钟不瀑布杀，快速拉到 7%+，回踩不破开盘价，5 分钟量能 >= 昨日同期 1.5 倍 | 中 | 竞价、分时、量能对比 |
| 弱转强交易 | 市场与板块限制 | 仅绿灯且所属板块涨停 >= 5 家 | 中 | 市场状态、板块涨停数 |
| 仓位管理 | 总龙头仓位 | 绿灯 + 总龙头时 80%-100%，止损 -5% | 中 | 市场状态、龙头标签、仓位 |
| 仓位管理 | 板块龙头仓位 | 绿灯 + 板块龙头时 50%-70%，止损 -7% | 中 | 市场状态、板块龙头标签 |
| 仓位管理 | 黄灯一进二 | 20%-40%，强制分仓两只各 20% | 高 | 市场状态、持仓数量、单票仓位 |
| 风控惩罚 | 模式外交易处罚 | 1 次模式外交易次日空仓，连续 2 次停实盘 1 周 | 中 | 信号归因、交易标签、日期 |
| 风控惩罚 | 连续亏损处罚 | 连续两周亏损 > 15%，停实盘 1 个月 | 高 | 周度收益率、日期 |

### 1.3 仍依赖主观判断的规则点

下列项目在原始文档中具有经验价值，但尚未被定义成可复算、可回测的一致口径，必须在下一版规范中转写成字段或算法。

| 主观点 | 当前表述 | 主观来源 | 为什么不能直接程序化 |
| :--- | :--- | :--- | :--- |
| 跌停潮 | “无跌停潮”/“有跌停潮” | 盘感与经验判断 | 未定义跌停家数阈值、跌停占比、封死率、是否排除 ST |
| 最高板核按钮 | “最高板核按钮” | 龙头情绪判断 | 未定义是一字跌停、竞价核、开盘秒跌停还是大幅低开 |
| 总龙头 | “可满仓总龙头”/“非总龙头” | 题材地位判断 | 未定义按连板高度、成交额、辨识度还是板块带动性认定 |
| 板块龙头 | “板块龙头” | 题材内部排序 | 未定义板块归属、龙头切换、同身位并列时如何处理 |
| 套牢盘密度 | “15% 空间内无超过 90 天密集成交区” | 筹码结构经验 | 未定义密集区算法、峰值阈值、使用哪类筹码分布数据 |
| 午后偷鸡板 | “13:30 后直线拉升封板” | 分时形态主观识别 | “直线拉升”缺少斜率、持续分钟数、回撤幅度定义 |
| 封单质量 | “主力实力存疑” | 盘口经验 | 仅给出静态封单金额/手数，未定义撤单率、回封次数、量价关系 |
| 不瀑布杀 | “开盘不瀑布杀” | 分时体感 | 未定义 1-5 分钟内的最大回撤和回撤速度阈值 |
| 分时守住开盘价 | “回踩不破开盘价” | 盘中观察 | 未定义允许瞬时跌破还是必须收盘在其上 |
| 分时均线向上 | “分时均线向上” | 走势观察 | 未定义使用 VWAP 还是均价线，以及斜率阈值 |
| 异常撤单 | “竞价封单大幅撤单” | 盘口观察 | 未定义比较窗口、撤单比例、样本频率 |
| 板块效应 | “同板块 >= 2/3/5 只涨停” | 题材聚合经验 | 未定义板块来源、股票多概念归属、临时热点如何映射 |
| 烂板 | “烂板 / 炸板回封” | 涨停质量判断 | 未定义开板次数、开板持续时长、炸板深度 |
| 龙头豁免 | “14:55 未涨停非总龙头强制卖” | 辨识度判断 | 未定义龙头可被豁免到什么条件、是否需要缩量/换手配合 |
| 模式外交易 | “发生 1 次模式外交易” | 纪律判断 | 未定义什么算模式外，例如试错单、午后打板、低吸等 |

### 1.4 潜在胜率漏洞与规则缺口

以下问题不是“文档不够完整”这么简单，而是会直接影响策略样本纯度、执行一致性和实际胜率。

| 编号 | 漏洞/缺口 | 当前表现 | 可能后果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| V1 | 情绪闸门区间存在重叠与缺口 | 黄灯未纳入晋级率条件，红灯含“或最高板核按钮”，绿灯要求“必须全部满足” | 同一天可能被不同执行者判成不同仓位档位，导致回测与实盘失真 | 高 |
| V2 | 多个核心概念未标准化 | 跌停潮、总龙头、板块龙头、核按钮均无统一口径 | 策略无法批量回测，也无法稳定复现 | 高 |
| V3 | 一进二评分卡分值体系不自洽 | 表头写“满分 30 分”，但现有权重设计满分实际为 27 分 | 入池阈值 21 分的统计意义失真，后续优化无法比较 | 高 |
| V4 | 首板质量与次日入场的因果链不完整 | 首板评分后只给“入池”，未定义多候选排序、最大候选数、同题材去重 | 容易在强日同时出手过多、分散掉最优样本 | 高 |
| V5 | 买点偏后，追涨成本高 | 规定 5 分钟内拉到 7%+，再在 7%-8% 追 40% 仓位 | 如果次日溢价均值不足，容易把高质量样本做成低盈亏比 | 高 |
| V6 | 对“高开直接秒板”只有放弃，没有替代方案 | 秒板样本全部放弃，炸板回封又允许满仓参与 | 会系统性错失最强承接样本，样本偏向次强甚至后排 | 中 |
| V7 | 卖出规则存在层级冲突 | 总止损 -7%，总龙头止损 -5%，同时又有破开盘价、破均线、尾盘未板清仓 | 多个卖出条件同时触发时缺少优先级，实盘执行会漂移 | 高 |
| V8 | 持股周期定义互相冲突 | “持股 1 天，最多 2 天” 与 “第三天竞价卖出” 同时存在 | 系统边界不清，回测持有期难以统一 | 高 |
| V9 | 竞价量能阈值表述为“约 5%-10%” | 使用“约”而不是明确上下界或分层 | 无法稳定筛选，手工执行偏差大 | 中 |
| V10 | 盘中承接定义缺少时间和价格容忍度 | “不瀑布杀”“守住开盘价”“分时均线向上”均无数值化 | 盘中自动监控难以实现，复盘也难对错归因 | 高 |
| V11 | 风控只定义结果，不定义交易次数上限 | 没有限制单日出手次数、同题材最大暴露、单票滑点容忍 | 情绪好时容易过度交易，放大回撤 | 中 |
| V12 | 缺少涨停失败分型 | 只写“炸板后再次回封可打”，未定义炸板深度、回封次数、回封时量能 | 容易把高位出货板误判成强回封 | 高 |
| V13 | 缺少集合竞价与开盘异常场景处理 | 未定义一字板、超预期高开 >=7%、低开后快速翻红、指数突发跳水等分支 | 大量边界行情没有执行规范 | 高 |
| V14 | 情绪闸门只看日初，不看日内切换 | 文档没有“黄转红”“绿转黄”降档机制 | 实盘中午后情绪塌陷时仍可能按早盘仓位执行 | 中 |
| V15 | 风控处罚依赖人工认定 | 模式外交易、反思、停实盘等未与交易记录结构化绑定 | 纪律可以写下来，但无法被系统自动监督 | 中 |

### 1.5 建议作为下一阶段优先落地的改造项

为了先解决“胜率”而不是先追求交易次数，建议按以下顺序推进：

1. 先统一市场状态口径：补全 `跌停潮`、`最高板核按钮`、`总龙头`、`板块龙头` 的量化定义。
2. 重写一进二评分卡：修正总分、拆成可统计因子，并加入候选池排序与题材去重规则。
3. 重写次日入场状态机：把竞价、开盘 5 分钟、首次上冲、回踩、回封拆成互斥分支。
4. 给卖出规则建立优先级：硬止损 > 结构破坏 > 时间止盈 > 持仓天数退出，避免冲突。
5. 补全边界行情处理：一字板、超预期高开、低开翻红、炸板深回封、午后情绪退潮。
6. 把纪律项改写成可审计事件：每笔交易必须关联模式标签、市场状态、入场分支、违规标记。

## 2. 策略优化方案

### 2.1 优化目标与设计原则

本章将原始经验规则改写为可执行策略规范，目标顺序固定为：`先提高胜率` -> `再控制回撤` -> `最后追求收益弹性`。所有策略更新都必须满足以下原则：

1. 先减少低质量出手，再讨论放宽样本数量。
2. 所有主观描述必须被翻译成字段、阈值、排序逻辑或状态机分支。
3. `一进二打板` 为主策略，`2板弱转强` 为辅助策略；辅助策略不得绕过主策略的市场风控。
4. 买点、卖点、仓位、处罚必须共享同一套事件记录，禁止人工“事后解释”。
5. 当市场环境与个股信号冲突时，以 `市场环境` 为优先级更高的约束。

### 2.2 统一术语与量化定义

为消除执行歧义，以下主观概念统一改写为系统口径。

| 术语 | 统一定义 | 用途 |
| :--- | :--- | :--- |
| `跌停潮` | 非 `ST` 跌停股数 `>= 15`，且跌停股数 / 当日涨停股数 `>= 0.30`；或连续跌停股数 `>= 5` | 情绪红灯、禁做窗口 |
| `最高板核按钮` | 前一交易日市场最高连板股，在当日竞价至开盘 `10` 分钟内出现 `开盘涨跌幅 <= -5%` 或首次触及跌停 | 情绪退潮确认 |
| `总龙头` | 满足 `全市场最高连板身位`，且 `所属主线板块强度` 排名前 `3`，且 `近 3 日成交额分位` 位于同身位样本前 `30%` | 仓位豁免、尾盘持有豁免 |
| `板块龙头` | 在同一主线板块内，同时满足 `连板高度最高`，若并列则取 `成交额更高 + 首次封板更早` 的个股 | 板块去重、仓位放大 |
| `午后偷鸡板` | 首次封板时间 `>= 13:30`，且封板前 `15` 分钟累计涨幅 `>= 8%`，中途无超过 `2` 次有效回踩 | 一进二首板过滤 |
| `烂板` | 当日涨停后开板次数 `>= 2`，或首次封板到最后封板间隔 `>= 30` 分钟 | 弱转强前置样本 |
| `不瀑布杀` | 开盘后前 `5` 分钟内，最低价相对开盘价回撤 `<= 2.0%`，且单分钟跌幅绝对值不超过 `1.2%` | 次日承接确认 |
| `守住开盘价` | 开盘后前 `5` 分钟内若跌破开盘价，允许瞬时跌破但连续停留不得超过 `2` 根 1 分钟 K 线，且 `5` 分钟收盘价需回到开盘价之上 | 承接确认 |
| `分时均线` | 统一使用分钟级 `VWAP`，禁止不同模块混用自定义均价线 | 回踩加仓、结构破坏 |
| `模式外交易` | 任意成交记录无法匹配到预定义 `strategy_family + entry_branch + market_regime` 组合，或在红灯日出手主策略，均记为违规 | 纪律处罚 |

### 2.3 市场环境状态机

市场状态从“早盘预判”改为“日内可切换状态机”，避免只在日初做一次判断。

#### 2.3.1 输入字段

| 字段 | 定义 |
| :--- | :--- |
| `limit_up_count` | 非 `ST` 涨停家数 |
| `limit_down_count` | 非 `ST` 跌停家数 |
| `max_board_height` | 市场最高连板数 |
| `yesterday_promotion_rate` | 昨日连板股今日继续连板比例 |
| `highest_board_nuke_flag` | 最高板核按钮标记 |
| `market_shock_flag` | 指数或情绪在 `30` 分钟内快速恶化的异常标记 |

#### 2.3.2 市场状态定义

| 状态 | 触发条件 | 仓位上限 | 允许策略 |
| :--- | :--- | :--- | :--- |
| `GREEN` | `limit_up_count >= 60` 且 `max_board_height >= 5` 且 `yesterday_promotion_rate >= 0.30` 且 `跌停潮 = false` 且 `highest_board_nuke_flag = false` | `100%` | `一进二`、`2板弱转强` |
| `YELLOW` | 不满足 `GREEN`，但 `limit_up_count` 在 `40-59`，`max_board_height >= 4`，`跌停潮 = false`，`highest_board_nuke_flag = false` | `40%` | 仅 `一进二` |
| `RED` | 其余全部情形，或 `跌停潮 = true`，或 `highest_board_nuke_flag = true` | `0-10%` | 不开新仓，仅允许观测单 |

#### 2.3.3 日内降档与恢复

1. `GREEN -> YELLOW`：若午后 `30` 分钟内涨停家数净减少 `>= 15`，或跌停家数净增加 `>= 8`。
2. `GREEN/YELLOW -> RED`：若触发 `跌停潮`、`最高板核按钮`、指数急跌伴随涨停池快速收缩。
3. 状态一旦降到 `RED`，当日不得恢复到 `GREEN`；最多恢复到 `YELLOW` 观察态，且不允许新开主仓。
4. 所有交易计划在发单前必须再次校验当前 `market_regime`，避免盘前计划与盘中执行脱节。

### 2.4 主策略 A：一进二打板

#### 2.4.1 策略目标

在 `T` 日识别高质量首板样本，在 `T+1` 借助竞价和开盘承接确认，只参与“有板块、有承接、有加速空间”的二板晋级。

#### 2.4.2 硬性过滤条件

任一条件不满足即淘汰：

| 条件 | 阈值 |
| :--- | :--- |
| 市场状态 | 盘前预判不得为 `RED` |
| 流通市值 | `20e8 <= float_market_cap <= 200e8` |
| 标的限制 | 剔除 `ST`、上市未满 `60` 交易日、创业板 `300`、科创板、北交所 |
| 趋势过滤 | `close > MA250` |
| 股性激活 | 近 `60` 交易日内至少 `1` 次涨停 |
| 首板时点 | 非 `午后偷鸡板` |
| 首板封板资金 | `seal_amount >= 20,000,000` |
| 流动性 | `amount >= 150,000,000` |
| 异常样本剔除 | 财务雷、重大停牌复牌首日、公告型一字首板样本默认剔除 |

#### 2.4.3 首板质量评分卡

评分卡统一修正为满分 `30` 分，入池阈值默认 `>= 21` 分；若 `GREEN` 日候选过多，只保留前 `5` 名。

| 因子 | 分值规则 | 满分 |
| :--- | :--- | :--- |
| `first_limit_up_time_score` | `09:30-10:00 = 9`，`10:00-10:30 = 6`，`10:30-11:30 = 3`，其余 `0` | `9` |
| `seal_quality_score` | `未开板且 seal_amount 高 = 6`，`开板 1 次 = 4`，`开板 2 次 = 2`，其余 `0` | `6` |
| `turnover_score` | `5%-15% = 6`，`3%-5% 或 15%-20% = 4`，其余 `0` | `6` |
| `board_synergy_score` | 同题材涨停 `>= 3` 且板块涨幅居前 = `6`，同题材涨停 `2` 只 = `4`，独苗 = `1` | `6` |
| `liquidity_bonus_score` | `amount`、`float_market_cap`、`振幅` 综合满足优选区间 = `3`，一般 = `1`，过热/过冷 = `0` | `3` |

#### 2.4.4 候选池排序与去重

当入池样本超过可观察容量时，按以下顺序排序：

1. `首板总分` 降序。
2. 同分时优先 `主线板块` 而非孤立题材。
3. 同板块只保留 `1` 只主候选与 `1` 只备选，防止同题材集中踩雷。
4. 若存在 `总龙头补涨` 与 `杂毛跟风` 冲突，优先保留辨识度更高、首次封板更早的样本。

#### 2.4.5 次日入场状态机

一进二不再使用“看着强就上”的连续表述，而改为互斥分支：

| 分支 | 触发条件 | 默认动作 | 默认仓位 |
| :--- | :--- | :--- | :--- |
| `auction_reject` | `auction_gap_pct < 3%` 或 `> 6.5%`，或 `auction_volume_ratio < 4%` 或 `> 12%`，或竞价撤单异常 | 放弃 | `0%` |
| `first_surge_entry` | 开盘前 `5` 分钟满足 `不瀑布杀`、`守住开盘价`、`5` 分钟涨幅 `>= 5%` 且 `<= 7.5%` | 首次上冲买入 | `30%-40%` |
| `pullback_add` | 已触发 `first_surge_entry` 后，回踩 `VWAP` 不破且重新放量拐头 | 加仓 | 累计至 `50%-60%` |
| `reseal_entry` | 开盘后炸板，但炸板深度 `<= 4%`、回封时间不晚于 `10:30`、回封前 5 分钟成交额不弱于首次冲板时的 `80%` | 回封打板 | 累计至系统上限 |
| `instant_limit_skip` | 高开后 `2` 分钟内直接秒板且无有效换手 | 放弃，不排板 | `0%` |

#### 2.4.6 放弃条件

满足任一条件则候选直接失效：

1. 开盘前 `10` 分钟所属板块强度显著转弱，且板块涨停数低于 `2`。
2. 个股开盘后 `5` 分钟最大回撤超过 `2%`。
3. `5` 分钟换手未达到前一日全天换手的 `25%`。
4. 早盘前 `15` 分钟出现两次以上冲高回落，且每次回落均跌破 `VWAP`。
5. 市场状态在计划入场前降为 `RED`。

### 2.5 辅助策略 B：2板弱转强

#### 2.5.1 策略定位

该策略只在强市环境下作为补充，目的是参与“前一日分歧、次日超预期修复”的二板延续，不承担主收益任务。

#### 2.5.2 前一日弱板定义

候选必须同时满足以下三类条件中的至少两类：

| 类别 | 条件 |
| :--- | :--- |
| 涨停质量 | `烂板` 或首次封板时间 `>= 14:30` |
| 波动结构 | 当日振幅 `>= 8%` 且 `<= 18%` |
| 换手结构 | `turnover_pct` 在 `5%-15%` |
| 回封结构 | 出现炸板回封，但最后仍封住涨停 |

#### 2.5.3 次日转强确认

| 条件 | 阈值 |
| :--- | :--- |
| 市场状态 | 必须为 `GREEN` |
| 板块强度 | 所属板块涨停 `>= 5`，板块涨幅位居市场前列 |
| 高开幅度 | `auction_gap_pct >= 5%` 且 `<= 8%` |
| 承接质量 | 开盘 `3-5` 分钟无瀑布杀，且始终运行在开盘价附近上方 |
| 动量确认 | 开盘后快速拉升至 `7%+` |
| 回踩确认 | 回踩开盘价不破，且 `VWAP` 斜率向上 |
| 量能确认 | 前 `5` 分钟成交量 `>=` 昨日同期 `1.5` 倍 |

#### 2.5.4 交易与放弃规则

1. 默认只允许 `1` 次试错买入，首仓 `20%-30%`。
2. 仅当回踩确认成立时允许补到 `40%-50%`，不得在首次急拉时直接满仓。
3. 若高开后直接秒板且无回踩，视为 `观察样本`，不参与追板。
4. 若开盘后跌破开盘价并持续 `2` 根 1 分钟线未收回，则该样本作废。
5. 卖出规则完全继承第 `2.6` 节统一卖出优先级。

### 2.6 卖出规则与优先级状态机

所有策略共享同一卖出状态机，避免“谁先看到谁先卖”的漂移。

| 优先级 | 触发条件 | 动作 |
| :--- | :--- | :--- |
| `P0` 账户级熔断 | 当日账户收益率 `<= -10%` | 全部强平，当日停止交易 |
| `P1` 单票硬止损 | 普通仓位 `<= -7%`；总龙头豁免仓位 `<= -5%` | 立即清仓 |
| `P2` 跳空弱开止损 | 次日开盘涨跌幅 `< -3%`，且开盘后 `10` 分钟不翻红 | 清仓 |
| `P3` 结构破坏 | 跌破开盘价，或跌破 `VWAP` 并持续 `3` 分钟不收回 | 先减仓 `50%`；若同时跌破开盘价则直接清仓 |
| `P4` 时间止盈/止损 | `14:55` 前仍未涨停，且非 `总龙头` | 清仓 |
| `P5` 持仓天数强退 | 非 `总龙头` 持有至 `T+2` 开盘竞价 | 强制退出 |

补充规则：

1. 同一分钟命中多个卖点时，只执行优先级最高的动作。
2. 若减仓后在同日再次触发 `P3`，允许直接升级为清仓。
3. `总龙头` 的持有豁免只影响 `P4/P5`，不豁免 `P0-P3`。

### 2.7 仓位与风险控制

#### 2.7.1 仓位分配

| 场景 | 单票上限 | 单日总上限 | 备注 |
| :--- | :--- | :--- | :--- |
| `GREEN + 一进二主候选` | `60%` | `100%` | 最多 `2` 笔主仓 |
| `GREEN + 总龙头/最强分支` | `80%-100%` | `100%` | 需满足总龙头算法定义 |
| `GREEN + 弱转强` | `50%` | `100%` | 仅作辅助，不得与两只主仓同时满配 |
| `YELLOW + 一进二` | `20%-25%` | `40%` | 强制分仓，最多 `2` 只 |
| `RED` | `0-10%` | `0-10%` | 仅测试单，不计入正式样本 |

#### 2.7.2 出手次数与题材暴露

1. 单日正式交易不超过 `2` 笔建仓。
2. 同一题材最大暴露不超过当日总仓位的 `60%`。
3. 未通过候选池排序的样本，即使盘中走强也不得临时纳入正式交易。
4. 任何加仓动作都必须以已有盈利或结构确认作为前提，不允许亏损补仓。

#### 2.7.3 纪律处罚

| 事件 | 处罚 |
| :--- | :--- |
| `1` 次模式外交易 | 次日 `FORCED_FLAT`，只允许复盘不允许实盘 |
| 连续 `2` 次模式外交易 | 停止实盘 `5` 个交易日 |
| 连续两周净值回撤 `> 15%` | 停止实盘 `20` 个交易日，并回归模拟盘 |
| 未记录交易原因/截图/复盘标签 | 该笔交易标记为 `audit_failed`，不计入有效样本 |

### 2.8 交易审计与复盘字段

为保证策略、执行与纪律闭环，每笔交易都必须带上以下结构化标签：

| 字段 | 说明 |
| :--- | :--- |
| `strategy_family` | `first_board_to_second` / `weak_to_strong_2nd` |
| `market_regime_at_plan` / `market_regime_at_entry` | 计划时与成交时市场状态 |
| `candidate_rank` | 候选池排序名次 |
| `entry_branch` / `exit_branch` | 命中的具体状态机分支 |
| `board_name` / `board_strength_bucket` | 所属板块与板块强弱档位 |
| `rule_violation_flag` | 是否违规 |
| `violation_reason` | 违规原因 |
| `review_tag` | 如 `竞价过热`、`承接失败`、`卖飞`、`执行犹豫` |

若缺少以上字段，系统应拒绝将该笔交易记为“合规样本”。

## 3. 数据验证框架

### 3.1 本次核查范围与结论

本次数据能力核查基于本地已安装的 `akshare_one_mcp 0.3.9`、`akshare_one 0.4.1` 与 `akshare 1.18.55`。结论分两层：

1. `akshare_one_mcp` 当前对 A 股策略直接暴露的核心行情工具只有 `get_hist_data` 与 `get_realtime_data`。
2. 统一 MCP 接口做了字段标准化，优点是简单一致，缺点是把不少策略必需字段裁剪掉了。
3. 若只使用 MCP 当前暴露的标准化历史接口，`一进二`、`2板弱转强` 需要的 `成交额`、`换手率`、`涨跌幅`、`振幅`、`均价/VWAP`、`盘前竞价/盘前分时`、`涨停池/跌停池`、`板块成份` 等字段并不完整。
4. 底层 Akshare 原生接口已经具备大部分补数能力，但实测东方财富与新浪源都存在偶发 `RemoteDisconnected` / `ConnectionReset`，因此后续软件实现必须自带重试、缓存与降级逻辑。

### 3.2 当前 MCP 统一接口能力矩阵

| 接口 | 数据类型 | 形参支持的时间粒度 | 复权参数 | 标准化输出字段 | 当前可直接满足的用途 | 当前不足 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `get_hist_data` | 个股历史行情 | `minute`、`hour`、`day`、`week`、`month`、`year`，并支持 `interval_multiplier >= 1` | `none`、`qfq`、`hfq` | `timestamp`、`open`、`high`、`low`、`close`、`volume` | 基础 K 线、MA/EMA/RSI/MACD 等技术指标输入 | 丢失 `amount`、`turnover`、`pct_change`、`change`、`amplitude`、`vwap`；无法单靠它完成首板质量、竞价承接、换手确认 |
| `get_realtime_data` | 个股实时快照 | 无显式粒度；单次调用返回当前时刻快照 | 不适用 | `symbol`、`price`、`change`、`pct_change`、`volume`、`amount`、`open`、`high`、`low`、`prev_close`、`timestamp` | 盘中监控、持仓止损、最新价/涨跌幅跟踪 | 没有盘口委买委卖、封单、撤单、竞价明细、板块归属 |

### 3.3 底层 Akshare 可补充的 A 股行情接口

下表只保留与本策略直接相关、且本次已核查到源码字段定义的接口。

| 接口 | 数据域 | 时间粒度 | 关键原始字段 | 对应策略用途 |
| :--- | :--- | :--- | :--- | :--- |
| `stock_zh_a_hist` | 个股日线/周线/月线 | `daily` / `weekly` / `monthly` | `日期`、`股票代码`、`开盘`、`收盘`、`最高`、`最低`、`成交量`、`成交额`、`振幅`、`涨跌幅`、`涨跌额`、`换手率` | 年线过滤、流通性过滤、前一日首板质量、振幅约束、历史涨停样本构建 |
| `stock_zh_a_hist_min_em(period='1')` | 个股 1 分钟分时 | 1 分钟，近 5 个交易日 | `时间`、`开盘`、`收盘`、`最高`、`最低`、`成交量`、`成交额`、`均价` | 开盘 5 分钟承接、瀑布杀识别、分时均价线/VWAP、拉升速度 |
| `stock_zh_a_hist_min_em(period in '5/15/30/60')` | 个股多分钟分时 | 5 / 15 / 30 / 60 分钟 | `时间`、`开盘`、`收盘`、`最高`、`最低`、`涨跌幅`、`涨跌额`、`成交量`、`成交额`、`振幅`、`换手率` | 分钟级特征聚合、盘中节奏识别、回封前后量价对比 |
| `stock_zh_a_hist_pre_min_em` | 个股盘前 + 盘中分时 | 单交易日分钟级 | `时间`、`开盘`、`收盘`、`最高`、`最低`、`成交量`、`成交额`、`最新价` | 竞价结束到开盘前后的承接观测，补足盘前窗口 |
| `stock_zh_a_spot_em` | 全市场实时快照 | 实时快照 | `代码`、`名称`、`最新价`、`涨跌幅`、`涨跌额`、`成交量`、`成交额`、`振幅`、`最高`、`最低`、`今开`、`昨收`、`量比`、`换手率`、`市盈率-动态`、`市净率`、`总市值`、`流通市值`、`涨速`、`5分钟涨跌`、`60日涨跌幅`、`年初至今涨跌幅` | 盘中候选池扫描、流通市值过滤、量比/换手率/涨速监控 |
| `stock_zt_pool_em` | 当日涨停股池 | 交易日级，盘中/盘后刷新 | `代码`、`名称`、`涨跌幅`、`最新价`、`成交额`、`流通市值`、`总市值`、`换手率`、`封板资金`、`首次封板时间`、`最后封板时间`、`炸板次数`、`涨停统计`、`连板数`、`所属行业` | 首板/连板样本、封板时间、炸板次数、封板质量、连板高度 |
| `stock_zt_pool_previous_em` | 昨日涨停股池 | 交易日级 | `代码`、`名称`、`涨跌幅`、`最新价`、`涨停价`、`成交额`、`流通市值`、`总市值`、`换手率`、`涨速`、`振幅`、`昨日封板时间`、`昨日连板数`、`涨停统计`、`所属行业` | 昨日首板/连板复盘、晋级率统计、次日承接分层 |
| `stock_zt_pool_dtgc_em` | 当日跌停股池 | 交易日级 | `代码`、`名称`、`涨跌幅`、`最新价`、`成交额`、`流通市值`、`总市值`、`动态市盈率`、`换手率`、`封单资金`、`最后封板时间`、`板上成交额`、`连续跌停`、`开板次数`、`所属行业` | `跌停潮` 定义、情绪闸门红灯、退潮窗口 |
| `stock_board_industry_name_em` | 行业板块总览 | 实时快照 | `板块名称`、`板块代码`、`最新价`、`涨跌额`、`涨跌幅`、`总市值`、`换手率`、`上涨家数`、`下跌家数`、`领涨股票`、`领涨股票-涨跌幅` | 行业强弱、行业扩散度、板块热度排序 |
| `stock_board_industry_cons_em` | 行业板块成份股 | 实时快照 | `代码`、`名称`、`最新价`、`涨跌幅`、`涨跌额`、`成交量`、`成交额`、`振幅`、`换手率`、`市盈率-动态`、`最高`、`最低`、`今开`、`昨收`、`市净率` | 同板块涨停数、板块联动、题材内龙头/跟风区分 |
| `stock_board_concept_name_em` | 概念板块总览 | 实时快照 | `板块名称`、`板块代码`、`最新价`、`涨跌额`、`涨跌幅`、`总市值`、`换手率`、`上涨家数`、`下跌家数`、`领涨股票`、`领涨股票-涨跌幅` | 热点概念强度、主线板块识别 |
| `stock_board_concept_cons_em` | 概念板块成份股 | 实时快照 | `代码`、`名称`、`最新价`、`涨跌幅`、`涨跌额`、`成交量`、`成交额`、`振幅`、`换手率`、`市盈率-动态`、`最高`、`最低`、`今开`、`昨收`、`市净率` | 同题材涨停家数、板块效应统计、同身位去重 |

### 3.4 面向策略实现的数据字典

后续所有特征工程都统一落在以下逻辑实体上，而不是直接散落使用第三方字段名。

#### 3.4.1 `SecurityDailyBar`

| 逻辑字段 | 类型 | 来源接口 | 原始字段 | 粒度 | 用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `trade_date` | `date` | `stock_zh_a_hist` | `日期` | 日 | 全部日线回测主键 |
| `symbol` | `string` | `stock_zh_a_hist` | `股票代码` | 日 | 个股主键 |
| `open` | `float` | `stock_zh_a_hist` | `开盘` | 日 | 缺口与高开识别 |
| `close` | `float` | `stock_zh_a_hist` | `收盘` | 日 | 首板成立、MA250、收益统计 |
| `high` | `float` | `stock_zh_a_hist` | `最高` | 日 | 振幅、炸板深度 |
| `low` | `float` | `stock_zh_a_hist` | `最低` | 日 | 波动与回撤 |
| `volume` | `float` | `stock_zh_a_hist` | `成交量` | 日 | 放量识别 |
| `amount` | `float` | `stock_zh_a_hist` | `成交额` | 日 | 流动性、首板质量 |
| `amplitude_pct` | `float` | `stock_zh_a_hist` | `振幅` | 日 | 烂板、弱转强前一日波动 |
| `pct_change` | `float` | `stock_zh_a_hist` | `涨跌幅` | 日 | 涨停判定、收益归因 |
| `change_amount` | `float` | `stock_zh_a_hist` | `涨跌额` | 日 | 辅助校验 |
| `turnover_pct` | `float` | `stock_zh_a_hist` | `换手率` | 日 | 一进二换手打分、弱转强过滤 |

#### 3.4.2 `SecurityMinuteBar`

| 逻辑字段 | 类型 | 来源接口 | 原始字段 | 粒度 | 用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `bar_time` | `datetime` | `stock_zh_a_hist_min_em` / `stock_zh_a_hist_pre_min_em` | `时间` | 1 / 5 / 15 / 30 / 60 分钟 | 分时主键 |
| `open` | `float` | 同上 | `开盘` | 分钟 | 开盘承接 |
| `close` | `float` | 同上 | `收盘` | 分钟 | 拉升/回落结束价 |
| `high` | `float` | 同上 | `最高` | 分钟 | 冲板瞬时强度 |
| `low` | `float` | 同上 | `最低` | 分钟 | 瀑布杀、回踩深度 |
| `volume` | `float` | 同上 | `成交量` | 分钟 | 放量确认 |
| `amount` | `float` | 同上 | `成交额` | 分钟 | 量价强度 |
| `vwap` | `float` | `stock_zh_a_hist_min_em(period='1')` | `均价` | 1 分钟 | 分时均线、回踩不破均价线 |
| `pct_change` | `float` | `stock_zh_a_hist_min_em(period in '5/15/30/60')` | `涨跌幅` | 多分钟 | 5 分钟拉升速度 |
| `change_amount` | `float` | 同上 | `涨跌额` | 多分钟 | 辅助校验 |
| `amplitude_pct` | `float` | 同上 | `振幅` | 多分钟 | 炸板深度 |
| `turnover_pct` | `float` | 同上 | `换手率` | 多分钟 | 盘初换手确认 |

#### 3.4.3 `RealtimeQuoteSnapshot`

| 逻辑字段 | 类型 | 来源接口 | 原始字段 | 刷新频率 | 用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `symbol` | `string` | `get_realtime_data` / `stock_zh_a_spot_em` | `symbol` / `代码` | 秒级到分钟级轮询 | 证券主键 |
| `price` | `float` | 同上 | `price` / `最新价` | 同上 | 实时盈亏、止损 |
| `change_amount` | `float` | 同上 | `change` / `涨跌额` | 同上 | 异动速度 |
| `pct_change` | `float` | 同上 | `pct_change` / `涨跌幅` | 同上 | 高开/冲高/回落 |
| `volume` | `float` | 同上 | `volume` / `成交量` | 同上 | 放量确认 |
| `amount` | `float` | 同上 | `amount` / `成交额` | 同上 | 盘中成交强度 |
| `open` | `float` | 同上 | `open` / `今开` | 同上 | 破开盘价清仓 |
| `high` | `float` | 同上 | `high` / `最高` | 同上 | 冲板高度 |
| `low` | `float` | 同上 | `low` / `最低` | 同上 | 急杀监控 |
| `prev_close` | `float` | 同上 | `prev_close` / `昨收` | 同上 | 竞价高开比例 |
| `turnover_pct` | `float` | `stock_zh_a_spot_em` | `换手率` | 同上 | 盘中换手 |
| `volume_ratio` | `float` | `stock_zh_a_spot_em` | `量比` | 同上 | 盘中活跃度 |
| `speed_pct` | `float` | `stock_zh_a_spot_em` | `涨速` | 同上 | 异动扫描 |
| `five_min_change_pct` | `float` | `stock_zh_a_spot_em` | `5分钟涨跌` | 同上 | 5 分钟趋势监控 |
| `float_market_cap` | `float` | `stock_zh_a_spot_em` | `流通市值` | 同上 | 样本过滤 |

#### 3.4.4 `LimitUpEvent`

| 逻辑字段 | 类型 | 来源接口 | 原始字段 | 粒度 | 用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `trade_date` | `date` | `stock_zt_pool_em` / `stock_zt_pool_previous_em` | 外部传入查询日 | 日 | 涨停事件主键 |
| `symbol` | `string` | 同上 | `代码` | 日 | 个股主键 |
| `limit_up_count` | `int` | `stock_zt_pool_em` | `连板数` | 日 | 连板高度 |
| `first_limit_up_time` | `string` | `stock_zt_pool_em` | `首次封板时间` | 日 | 首板质量 |
| `last_limit_up_time` | `string` | `stock_zt_pool_em` | `最后封板时间` | 日 | 回封/烂板区分 |
| `board_break_count` | `int` | `stock_zt_pool_em` | `炸板次数` | 日 | 烂板识别 |
| `seal_amount` | `float` | `stock_zt_pool_em` | `封板资金` | 日 | 封单质量 |
| `limit_up_stat` | `string` | `stock_zt_pool_em` / `stock_zt_pool_previous_em` | `涨停统计` | 日 | `N 天 M 板` 结构化解析 |
| `industry_name` | `string` | 同上 | `所属行业` | 日 | 板块归属 |
| `yesterday_limit_up_time` | `string` | `stock_zt_pool_previous_em` | `昨日封板时间` | 日 | 次日晋级研究 |
| `yesterday_board_count` | `int` | `stock_zt_pool_previous_em` | `昨日连板数` | 日 | 身位分层 |

#### 3.4.5 `LimitDownEvent`

| 逻辑字段 | 类型 | 来源接口 | 原始字段 | 粒度 | 用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `trade_date` | `date` | `stock_zt_pool_dtgc_em` | 外部传入查询日 | 日 | 跌停事件主键 |
| `symbol` | `string` | `stock_zt_pool_dtgc_em` | `代码` | 日 | 个股主键 |
| `seal_amount` | `float` | `stock_zt_pool_dtgc_em` | `封单资金` | 日 | 跌停封单强度 |
| `last_limit_down_time` | `string` | `stock_zt_pool_dtgc_em` | `最后封板时间` | 日 | 情绪尾盘恶化 |
| `board_trade_amount` | `float` | `stock_zt_pool_dtgc_em` | `板上成交额` | 日 | 跌停承接质量 |
| `continuous_limit_down` | `int` | `stock_zt_pool_dtgc_em` | `连续跌停` | 日 | 恐慌强度 |
| `board_break_count` | `int` | `stock_zt_pool_dtgc_em` | `开板次数` | 日 | 跌停反复开板 |

#### 3.4.6 `BoardStrengthSnapshot`

| 逻辑字段 | 类型 | 来源接口 | 原始字段 | 粒度 | 用途 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `board_type` | `enum` | 行业/概念接口 | 固定枚举 `industry` / `concept` | 实时快照 | 统一板块类型 |
| `board_code` | `string` | `stock_board_industry_name_em` / `stock_board_concept_name_em` | `板块代码` | 实时快照 | 板块主键 |
| `board_name` | `string` | 同上 | `板块名称` | 实时快照 | 板块名称 |
| `pct_change` | `float` | 同上 | `涨跌幅` | 实时快照 | 板块强弱 |
| `up_count` | `int` | 同上 | `上涨家数` | 实时快照 | 扩散度 |
| `down_count` | `int` | 同上 | `下跌家数` | 实时快照 | 分歧度 |
| `leader_symbol` | `string` | 同上 | `领涨股票` | 实时快照 | 龙头定位 |
| `leader_pct_change` | `float` | 同上 | `领涨股票-涨跌幅` | 实时快照 | 龙头强度 |

### 3.5 对策略规则的直接支撑与当前缺口

#### 已能支撑的规则

1. `流通市值`、`量比`、`换手率`、`高开幅度`、`5 分钟涨跌`、`开盘价/昨收价` 等基础过滤。
2. `首板封板时间`、`炸板次数`、`连板数`、`封板资金`、`跌停股池数量` 等情绪与板块事件统计。
3. 分钟线级别的 `开盘承接`、`拉升速度`、`回踩深度`、`VWAP/均价线` 派生特征。

#### 仍未闭环的规则

1. `封单手数`、`撤单率`、`竞价未匹配量`、`竞价封单变化` 这类逐笔/盘口字段，当前已核查接口中没有稳定统一来源。
2. `总龙头`、`板块龙头` 仍需基于连板高度、成交额、板块扩散、领涨强度做二次算法定义，而不是直接读取单一字段。
3. `套牢盘密度`、`筹码峰` 相关信息，当前能力清单中没有直接字段，若要纳入需另找筹码分布数据源。

### 3.6 数据接入改造要求

为让后续回测与实盘共用同一份数据契约，数据层必须按以下方式实现：

1. 保留 `akshare_one_mcp` 当前统一字段，作为通用 OHLCV 与实时快照入口。
2. 新增 MCP 工具或本地封装，直接暴露下列原生接口：`stock_zh_a_hist`、`stock_zh_a_hist_min_em`、`stock_zh_a_hist_pre_min_em`、`stock_zh_a_spot_em`、`stock_zt_pool_em`、`stock_zt_pool_previous_em`、`stock_zt_pool_dtgc_em`、`stock_board_industry_name_em`、`stock_board_industry_cons_em`、`stock_board_concept_name_em`、`stock_board_concept_cons_em`。
3. 统一把所有时间字段转换为 `Asia/Shanghai` 时区，并明确 `trade_date` 与 `bar_time` 分层，不允许混用字符串时间。
4. 对东方财富、同花顺、新浪源统一增加 `3 次重试 + 本地缓存 + 失败回退`，否则回测样本和盘中监控都会受偶发断连污染。
5. 所有第三方字段进入策略引擎前，必须先映射到本节定义的逻辑字段名，禁止在规则代码中直接引用第三方中文列名。

### 3.7 验证闭环与参数迭代规范

本节定义 `样本构建 -> 特征提取 -> 信号生成 -> 撮合回测 -> 指标归因 -> 参数迭代` 的统一流程。后续所有规则优化、阈值调整和策略收缩，都必须基于本节输出的客观证据，禁止以单日案例、盘感或少量截图直接改规则。

#### 3.7.1 样本构建

验证样本按三层组织，确保既能研究市场环境，也能研究单票信号质量：

| 样本层级 | 主键 | 样本定义 | 主要用途 |
| :--- | :--- | :--- | :--- |
| `MarketDaySample` | `trade_date` | 单个交易日的全市场情绪快照 | 验证 `绿灯/黄灯/红灯` 与策略收益的关系 |
| `BoardDaySample` | `trade_date + symbol + strategy_family` | 某只股票在某交易日是否满足 `一进二` 或 `2板弱转强` 的前置条件 | 研究候选池过滤、评分和放弃条件 |
| `TradeSample` | `entry_date + symbol + branch_id` | 某条具体入场分支在回测中实际成交后的完整交易记录 | 研究盈亏比、回撤、卖点和仓位规则 |

样本构建口径统一如下：

1. 时间窗口默认使用 `2024-01-01` 起的 A 股样本；首版结论优先覆盖 `2024-2026` 的不同情绪阶段。
2. 股票池默认仅含沪深主板与中小盘可交易标的；剔除 `ST`、退市整理、新股上市未满 `60` 个交易日、长期停牌样本。
3. `一进二` 样本以 `T 日首板成立、T+1 日观察晋级` 为单位构建；`2板弱转强` 样本以 `T 日弱板、T+1 日转强` 为单位构建。
4. 每个样本必须同时保存 `准入前特征`、`触发时特征`、`交易后结果标签` 三组字段，禁止只保留命中样本。
5. 未触发买点但满足前置条件的候选也必须入库，标记为 `not_triggered`，用于区分“选股差”与“触发差”。
6. 退潮日、指数突发跳水、涨停池快速收缩等异常市场事件不能删样本，只能额外打上 `exception_tag`。

#### 3.7.2 特征与标签构建

每条 `BoardDaySample` 至少包含以下四类特征：

| 特征组 | 必备字段/派生项 | 说明 |
| :--- | :--- | :--- |
| 市场情绪 | `limit_up_count`、`limit_down_count`、`max_board_height`、`yesterday_promotion_rate`、`market_regime` | 用于验证不同情绪档位的胜率差异 |
| 板块强度 | `board_name`、`board_pct_change`、`board_limit_up_count`、`board_leader_strength` | 用于区分主线、跟风与孤立涨停 |
| 个股质量 | `float_market_cap`、`turnover_pct`、`amount`、`first_limit_up_time`、`board_break_count`、`seal_amount` | 用于首板质量评分与淘汰低质量样本 |
| 次日承接 | `auction_gap_pct`、`auction_volume_ratio`、`open_5m_return`、`open_5m_drawdown`、`open_5m_turnover_pct`、`vwap_support_flag` | 用于判断是否具备真实入场价值 |

每条样本都必须生成标准化结果标签，至少包含：

| 标签 | 定义 |
| :--- | :--- |
| `next_open_return` | 以 `T+1` 开盘价相对 `T` 收盘价的收益率 |
| `entry_to_high_return` | 买入后至持仓周期内最高价的最大浮盈 |
| `entry_to_low_return` | 买入后至卖出前最低价的最大浮亏 |
| `exit_return` | 按策略卖出规则撮合后的实际收益率 |
| `holding_minutes` / `holding_days` | 实际持有时长 |
| `hit_limit_up_after_entry` | 买入后是否再次触板/封板 |
| `failed_reseal_flag` | 炸板后是否回封失败 |
| `open_support_success_flag` | 开盘承接条件是否最终兑现 |

#### 3.7.3 回测撮合规则

回测引擎采用事件驱动撮合，按 `竞价 -> 开盘前 5 分钟 -> 盘中首次冲高 -> 回踩 -> 回封 -> 收盘/次日` 的顺序执行。为避免纸面收益失真，撮合必须遵守以下规则：

1. 买入触发只允许来自已定义的分支状态机，例如 `auction_entry`、`first_surge_entry`、`pullback_add`、`reseal_entry`，不得使用“当日最低价附近买到”这类未来函数。
2. 分钟级信号统一在下一根可成交分钟 `open` 成交；如果该分钟直接一字涨停或无成交量，则视为 `unfilled`，不得假设成交。
3. 默认交易成本使用 `佣金 + 印花税 + 双边滑点` 的固定模型；滑点首版可按 `0.2%-0.5%` 做情景分层，后续再与真实成交对账。
4. 卖出优先级固定为 `账户熔断 > 单票硬止损 > 结构破坏 > 时间止盈/尾盘退出 > 持仓天数强退`，回测与实盘必须共用同一优先级。
5. 若出现跳空低开直接跌破止损位，成交价取开盘后首个可成交价，不得按止损线理想成交。
6. 对涨停未开板、跌停封死、临停、午后流动性骤降等无法成交情形，必须记录 `liquidity_block_reason`，并计入可执行性分析。
7. 仓位撮合必须受 `market_regime`、单票上限、单日出手次数、同题材最大暴露约束；评分最高也不能绕过仓位风控。

回测输出的每笔交易都必须落成结构化记录：

| 字段 | 含义 |
| :--- | :--- |
| `sample_id` | 对应原始候选样本 |
| `strategy_family` | `first_board_to_second` / `weak_to_strong_2nd` |
| `entry_branch` / `exit_branch` | 命中的入场与卖出分支 |
| `entry_time` / `entry_price` / `entry_reason` | 入场时点、价格与触发原因 |
| `exit_time` / `exit_price` / `exit_reason` | 离场时点、价格与退出原因 |
| `position_pct` | 实际使用仓位 |
| `gross_return` / `net_return` | 毛收益与扣成本后净收益 |
| `max_favorable_excursion` / `max_adverse_excursion` | 持仓期间最大顺向/逆向波动 |
| `rule_violation_flag` | 是否出现规则冲突或无效撮合 |

#### 3.7.4 核心指标体系

策略评估不能只看总收益，必须至少同时输出以下四组指标：

| 指标组 | 核心指标 | 用途 |
| :--- | :--- | :--- |
| 收益质量 | `win_rate`、`avg_win`、`avg_loss`、`profit_factor`、`expectancy` | 判断模式本身是否具备正期望 |
| 风险承受 | `max_drawdown`、`consecutive_loss_count`、`entry_to_low_return_p95`、`volatility_of_equity` | 判断是否适合实盘承受 |
| 执行质量 | `fill_rate`、`unfilled_rate`、`slippage_impact`、`liquidity_block_rate` | 判断回测收益是否能真实成交 |
| 结构归因 | `market_regime_pnl`、`board_strength_bucket_pnl`、`first_limit_time_bucket_win_rate`、`auction_gap_bucket_expectancy` | 判断收益究竟来自哪类样本 |

除总体指标外，报告必须强制切片输出以下分布：

1. 按 `绿灯/黄灯/红灯` 切片的胜率、盈亏比、最大回撤。
2. 按 `首次封板时间`、`炸板次数`、`封板资金`、`竞价高开幅度`、`开盘 5 分钟回撤` 切片的收益分布。
3. 按 `板块涨停家数`、`板块涨幅`、`板块龙头强度` 切片的命中率与次日溢价。
4. 按 `买入分支` 与 `卖出分支` 切片的贡献度，识别真正赚钱的执行路径与拖后腿路径。

策略参数只有同时满足以下最低门槛，才允许进入候选版本：

| 约束项 | 最低要求 |
| :--- | :--- |
| 样本量 | 目标分组内成交样本不少于 `80` 笔；若不足，只能视为观察结论 |
| 正期望 | `expectancy > 0` 且 `profit_factor > 1.1` |
| 回撤可控 | `max_drawdown` 不得显著劣于当前基线版本 |
| 一致性 | 至少在最近 `3` 个滚动时间窗中有 `2` 个时间窗保持正收益 |
| 可执行性 | `fill_rate`、`slippage_impact` 不能说明收益完全依赖理想成交 |

#### 3.7.5 参数迭代方法

参数优化必须采用“先收缩规则，再微调阈值”的顺序，禁止直接做高维暴力调参。统一流程如下：

1. 先冻结交易分支，只调整单个因子阈值，例如 `竞价高开区间`、`开盘 5 分钟最大回撤`、`首板封板时间上限`。
2. 单次实验最多允许变动 `1-2` 个强相关参数，并记录 `实验目的 -> 预期改进指标 -> 实际结果`。
3. 训练、验证、观察三段样本按时间顺序切分，默认使用滚动窗口，例如 `训练 12 个月 + 验证 3 个月 + 观察 3 个月`。
4. 若参数在训练集显著改善、验证集持平、观察集回落，则按过拟合处理，不得上线。
5. 任何新增过滤条件都必须回答两个问题：`是否显著减少低质量样本`、`是否以过度牺牲成交数为代价`。
6. 每轮参数迭代结束后，必须生成 `ParameterChangeReport`，包含变更前后样本数、胜率、盈亏比、回撤、分布漂移与失败样本案例。

推荐的参数搜索方法如下：

| 场景 | 方法 | 约束 |
| :--- | :--- | :--- |
| 单阈值优化 | 分桶扫描 / 网格扫描 | 优先观察收益曲线是否平滑，避免只取尖峰值 |
| 强相关双参数 | 二维网格 + 热力图 | 只允许对少量强相关参数成对测试 |
| 多因子评分卡 | 固定权重后做逐项剔除 / 增量加入 | 先验证因子方向，再讨论权重细调 |
| 规则删减 | `ablation study` | 每次只删除一个规则，判断其真实贡献 |

#### 3.7.6 版本发布与证据门禁

每次策略升级都必须附带一份可复核的证据包，至少包括：

1. `样本定义说明`：本次使用的股票池、时间窗、异常样本处理方式。
2. `参数变更清单`：新增、删除、修改的规则与阈值。
3. `核心指标对比`：与上一基线版本的收益、回撤、可执行性对比。
4. `失败样本复盘`：至少列出 `10` 笔代表性亏损/错杀/未成交案例，确认问题来自规则还是市场噪声。
5. `上线结论`：明确标记为 `上线`、`继续观察` 或 `回退`，禁止只有图表没有结论。

## 4. 选股/回测软件产品设计

### 4.1 产品目标与边界

首版软件不是面向所有交易模式的通用量化平台，而是服务于《破晓系统》当前两类短线策略的“纪律执行器 + 数据验证器”。产品目标分为三层：

1. `盘前`：自动生成候选池、排序结果和交易计划。
2. `盘中`：实时监控触发条件、仓位约束、风险降档和卖出分支。
3. `盘后`：自动还原每笔交易、生成复盘证据、沉淀参数迭代输入。

明确不做的事情：

1. 不在首版追求全自动报单。
2. 不在首版支持多策略自由组合。
3. 不把 UI 美观性放在纪律执行与数据可追溯性之前。

### 4.2 系统模块总览

| 模块 | 职责 | 输入 | 输出 |
| :--- | :--- | :--- | :--- |
| `DataIngestion` | 拉取 Akshare/MCP 原始数据，负责重试与缓存 | 第三方接口 | 标准化原始表 |
| `MarketCache` | 统一存储日线、分钟线、涨停池、板块快照 | 标准化原始表 | 可查询的数据视图 |
| `FeatureEngine` | 生成市场情绪、板块强度、个股质量、竞价承接特征 | 原始表 + 缓存 | 特征表 |
| `StrategyEngine` | 执行市场状态机、候选池过滤、评分和触发逻辑 | 特征表 | `BoardCandidate`、`SignalEvent` |
| `BacktestEngine` | 按统一撮合规则回放历史信号 | 历史特征 + 策略规则 | `ExecutionRecord`、绩效指标 |
| `CandidateScorer` | 对候选池做排序、同题材去重与优先级决策 | 候选样本 | 候选清单 |
| `PreMarketPlanner` | 生成次日交易计划、预警清单与观察名单 | 候选清单 + 市场状态 | `TradePlan` |
| `IntradayMonitor` | 实时监控信号触发、仓位占用、风险切换、卖出条件 | 实时行情 + 交易计划 | 预警、成交建议、风控事件 |
| `ReviewReporter` | 生成盘后复盘报表、失败样本复盘、参数对比 | 成交记录 + 市场数据 | `ReviewReport` |

### 4.3 核心实体定义

#### 4.3.1 `MarketRegime`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `trade_date` | `date` | 交易日 |
| `regime` | `enum` | `GREEN` / `YELLOW` / `RED` |
| `limit_up_count` | `int` | 涨停家数 |
| `limit_down_count` | `int` | 跌停家数 |
| `max_board_height` | `int` | 最高连板 |
| `promotion_rate` | `float` | 连板晋级率 |
| `degrade_reason` | `string[]` | 降档原因 |
| `snapshot_time` | `datetime` | 状态快照时间 |

#### 4.3.2 `BoardCandidate`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `candidate_id` | `string` | 候选主键 |
| `trade_date` | `date` | 计划执行日 |
| `symbol` | `string` | 股票代码 |
| `strategy_family` | `enum` | 所属策略 |
| `board_name` | `string` | 板块名称 |
| `market_regime` | `enum` | 所属市场状态 |
| `hard_filter_pass` | `bool` | 是否通过硬过滤 |
| `score_total` | `float` | 总分 |
| `score_breakdown` | `json` | 分项分数 |
| `candidate_rank` | `int` | 排名 |
| `dedup_group` | `string` | 同题材去重组 |
| `status` | `enum` | `candidate` / `watch` / `rejected` / `triggered` |

#### 4.3.3 `SignalEvent`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `event_id` | `string` | 事件主键 |
| `candidate_id` | `string` | 对应候选 |
| `event_type` | `enum` | `auction_check`、`first_surge`、`pullback_confirm`、`reseal_confirm`、`risk_degrade`、`exit_trigger` |
| `event_time` | `datetime` | 事件时间 |
| `event_value` | `json` | 事件快照 |
| `decision` | `enum` | `pass` / `reject` / `warn` / `execute` |

#### 4.3.4 `TradePlan`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `plan_id` | `string` | 计划主键 |
| `trade_date` | `date` | 交易日 |
| `symbol` | `string` | 股票代码 |
| `strategy_family` | `enum` | 策略类型 |
| `planned_entry_branches` | `string[]` | 允许的买入分支 |
| `planned_position_pct` | `float` | 计划最大仓位 |
| `entry_constraints` | `json` | 竞价、承接、回踩阈值 |
| `exit_constraints` | `json` | 卖出规则与优先级 |
| `status` | `enum` | `planned` / `active` / `cancelled` / `completed` |

#### 4.3.5 `ExecutionRecord`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `execution_id` | `string` | 成交主键 |
| `plan_id` | `string` | 来源计划 |
| `entry_branch` | `string` | 实际买入分支 |
| `exit_branch` | `string` | 实际卖出分支 |
| `entry_time` / `exit_time` | `datetime` | 买卖时间 |
| `entry_price` / `exit_price` | `float` | 买卖价格 |
| `position_pct` | `float` | 实际仓位 |
| `gross_return` / `net_return` | `float` | 收益 |
| `mae` / `mfe` | `float` | 最大逆向/顺向波动 |
| `violation_flag` | `bool` | 是否违规 |

#### 4.3.6 `ReviewReport`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `report_id` | `string` | 报表主键 |
| `trade_date` | `date` | 复盘日期 |
| `regime_summary` | `json` | 市场环境总结 |
| `candidate_summary` | `json` | 候选池与触发概览 |
| `trade_summary` | `json` | 成交与收益概览 |
| `violation_summary` | `json` | 纪律违规统计 |
| `case_reviews` | `json[]` | 代表性样本 |
| `parameter_watchlist` | `json[]` | 后续需验证的参数点 |

### 4.4 盘前、盘中、盘后数据流

#### 4.4.1 盘前流程

1. `DataIngestion` 拉取前一日涨停池、跌停池、日线、板块数据与最近分钟线缓存。
2. `FeatureEngine` 计算首板质量、板块强度、市场情绪、股性激活等特征。
3. `StrategyEngine` 运行硬过滤与评分卡，输出候选池。
4. `CandidateScorer` 做排序、同题材去重，生成主候选与备选。
5. `PreMarketPlanner` 生成当日 `TradePlan`，并写出 `今日禁做条件`、`允许分支`、`仓位上限`。

#### 4.4.2 盘中流程

1. 竞价结束后，`IntradayMonitor` 校验 `auction_gap_pct`、`auction_volume_ratio` 与撤单异常。
2. 开盘后按分钟级行情持续更新 `SignalEvent`，检测 `不瀑布杀`、`守住开盘价`、`首次上冲`、`回踩确认`、`回封确认`。
3. 若市场状态降档，实时广播 `risk_degrade` 事件，并自动取消未触发计划。
4. 若已持仓，则持续检查 `P0-P5` 卖出状态机，并输出结构化退出建议。

#### 4.4.3 盘后流程

1. 固化当日全量快照、分钟线与交易事件日志。
2. `ReviewReporter` 自动还原每笔计划为何触发/未触发。
3. 生成 `ReviewReport`，强制回答“市场是否适合做”“是否按系统做”“亏损来自规则还是执行”三类问题。
4. 将成交样本写入 `BacktestEngine` 可复用的数据仓，用于后续参数迭代。

### 4.5 模块接口约束

为避免系统做大后模块耦合，首版就固定以下约束：

1. 策略模块禁止直接调用第三方接口，必须通过 `DataIngestion` 或缓存层取数。
2. 回测与实盘监控共享同一套 `StrategyEngine` 规则定义，只允许数据源与时间驱动方式不同。
3. 所有模块间传递对象时，优先使用本规范定义的逻辑字段名，而不是原始中文列名。
4. 任一模块若依赖临时人工输入，必须显式标记为 `manual_override` 并保留审计痕迹。

### 4.6 输出物与界面要求

首版软件至少需要稳定产出以下输出物，即使没有复杂前端也必须能导出：

| 输出物 | 最低要求 |
| :--- | :--- |
| `盘前候选清单` | 包含候选排名、总分、板块、买入分支、放弃条件、最大仓位 |
| `盘中监控面板` | 显示市场状态、活跃候选、持仓、风险事件、卖点预警 |
| `成交与风控日志` | 按时间顺序记录每个信号与每次动作 |
| `盘后复盘报表` | 包含候选命中率、交易盈亏、违规事件、失败样本 |
| `参数变更报告` | 用于回测迭代的对比证据 |

界面优先级约束：

1. 先保证 `信息完整与可追溯`，再考虑图表化。
2. 卖点预警、风险降档、违规提醒必须高于普通行情提示。
3. 每个候选标的必须能一键展开其 `评分明细 + 触发日志 + 放弃原因`。

### 4.7 存储与运行要求

1. 历史数据、特征、候选、事件、成交、报表必须分层存储，避免单表混杂。
2. 实时监控模块需要支持分钟级刷新；若数据源允许，可对关键标的提高刷新频率。
3. 对同一交易日的关键原始数据要做快照留存，保证日后复盘与回测可复现。
4. 数据拉取失败时优先使用最近可用缓存，并把该次降级写入系统日志。

### 4.8 MVP 交付顺序

建议按以下顺序落地，保证每一步都能形成可验证成果：

1. `MVP-1 数据底座`：打通日线、分钟线、涨停池、跌停池、板块数据与缓存。
2. `MVP-2 盘前选股`：实现市场状态、硬过滤、评分卡、候选排序、交易计划导出。
3. `MVP-3 回测闭环`：实现历史样本构建、事件驱动撮合、核心指标输出。
4. `MVP-4 盘中监控`：实现竞价检查、开盘承接监控、卖点预警、风险降档提示。
5. `MVP-5 盘后复盘`：生成复盘报表、失败案例、纪律统计与参数变更报告。

## 5. 开发任务拆解与里程碑

### 5.1 任务拆解原则

从本章开始，`Spec.md` 不再只是策略说明书，而是开发实施基线。任务拆解遵循以下原则：

1. 每个开发任务必须能映射到明确的输入、输出和验收标准。
2. 优先打通“数据 -> 候选 -> 计划 -> 触发 -> 成交 -> 复盘”的最短闭环，再增加复杂能力。
3. 所有任务默认按 `离线可验证` 优先，实时能力在已有离线逻辑稳定后接入。
4. 若同一能力同时服务回测与实盘，必须优先抽成共享模块。

### 5.2 首版开发工作包

| 工作包 | 核心目标 | 主要内容 | 依赖 |
| :--- | :--- | :--- | :--- |
| `WP-1 数据接入层` | 把第三方数据转成统一逻辑字段 | MCP/原生 Akshare 封装、重试、缓存、字段映射、时区统一 | 无 |
| `WP-2 行情与事件存储` | 形成可复盘数据底座 | 日线表、分钟线表、涨停池表、跌停池表、板块快照表、事件日志表 | `WP-1` |
| `WP-3 特征工程` | 生成策略所需全部特征 | MA250、股性激活、市场状态、板块强度、首板评分因子、竞价/开盘承接特征 | `WP-2` |
| `WP-4 盘前选股与计划` | 产出候选池与交易计划 | 硬过滤、评分卡、排序、同题材去重、计划导出 | `WP-3` |
| `WP-5 回测引擎` | 验证规则正期望与可执行性 | 样本构建、事件驱动撮合、交易成本、绩效统计、切片分析 | `WP-3` |
| `WP-6 盘中监控` | 让策略可实时执行与降档 | 竞价检查、入场分支监控、卖出状态机、仓位/风险广播 | `WP-4` |
| `WP-7 盘后复盘` | 形成纪律闭环与证据包 | 报表生成、失败样本、违规检测、参数变更报告 | `WP-5`、`WP-6` |

### 5.3 建议实施顺序

#### 5.3.1 阶段一：数据与离线验证优先

1. 完成 `WP-1` 与 `WP-2`，确保至少能稳定拉取并缓存 `2024-01-01` 以来的日线、分钟线、涨停池、跌停池、板块数据。
2. 完成 `WP-3` 的离线特征计算，先不接实时流，只要求历史样本能批量重算。
3. 完成 `WP-5` 的最小回测闭环，先支持 `一进二打板`，再扩展到 `2板弱转强`。

#### 5.3.2 阶段二：盘前执行工具

1. 完成 `WP-4`，输出可直接交易的 `盘前候选清单` 与 `TradePlan`。
2. 先支持命令行/表格导出，再考虑图形化界面。
3. 保证盘前计划可以和回测样本字段一一对应，避免口径分叉。

#### 5.3.3 阶段三：盘中与盘后闭环

1. 完成 `WP-6`，最少支持分钟级轮询与风险降档广播。
2. 完成 `WP-7`，自动生成 `ReviewReport` 与违规审计结果。
3. 当盘前、盘中、盘后统一使用同一套实体后，再讨论自动化下单或更复杂的可视化。

### 5.4 每个工作包的最小验收标准

#### 5.4.1 `WP-1 数据接入层`

| 验收项 | 标准 |
| :--- | :--- |
| 字段统一 | 所有外部字段可映射为本规范逻辑字段 |
| 容错能力 | 单接口失败可自动重试 `3` 次并记录日志 |
| 缓存能力 | 同日重复请求命中本地缓存，避免重复拉取 |
| 时区一致性 | 所有时间字段统一为 `Asia/Shanghai` |

#### 5.4.2 `WP-2 行情与事件存储`

| 验收项 | 标准 |
| :--- | :--- |
| 数据完整性 | 同一交易日各核心表可通过 `trade_date` 与 `symbol` 关联 |
| 快照可追溯 | 任一盘中状态都能回溯到对应原始快照 |
| 去重规则 | 同一主键重复写入时必须幂等 |

#### 5.4.3 `WP-3 特征工程`

| 验收项 | 标准 |
| :--- | :--- |
| 可重复计算 | 同一输入数据多次运行得到一致特征结果 |
| 特征覆盖 | 第 `2` 章和第 `3` 章要求的关键因子都已落库 |
| 异常处理 | 缺字段、停牌、无分钟线等异常样本有明确标记 |

#### 5.4.4 `WP-4 盘前选股与计划`

| 验收项 | 标准 |
| :--- | :--- |
| 候选准确性 | 能输出通过硬过滤后的候选及淘汰原因 |
| 排序透明性 | 每个候选包含总分与分项得分 |
| 去重有效性 | 同题材去重规则生效并可追溯 |
| 计划可执行 | 产出的 `TradePlan` 可直接供盘中监控使用 |

#### 5.4.5 `WP-5 回测引擎`

| 验收项 | 标准 |
| :--- | :--- |
| 撮合一致性 | 回测与实盘监控共用同一分支定义 |
| 成本建模 | 支持佣金、印花税、滑点参数 |
| 指标输出 | 能输出第 `3.7.4` 节要求的核心指标与切片 |
| 证据留痕 | 每笔交易都有结构化成交记录和触发原因 |

#### 5.4.6 `WP-6 盘中监控`

| 验收项 | 标准 |
| :--- | :--- |
| 信号实时性 | 候选状态、仓位占用、卖点预警可按分钟级刷新 |
| 风险降档 | 市场状态变化可即时取消未触发计划 |
| 状态留痕 | 每个盘中信号都写入 `SignalEvent` |

#### 5.4.7 `WP-7 盘后复盘`

| 验收项 | 标准 |
| :--- | :--- |
| 自动复盘 | 当日交易与候选可自动生成复盘报表 |
| 违规检测 | 模式外交易、缺失记录、风控绕过可自动识别 |
| 参数支撑 | 报表可直接进入参数迭代流程 |

### 5.5 建议的实现里程碑

| 里程碑 | 完成定义 |
| :--- | :--- |
| `M1 可回放数据底座` | 已能稳定拉取并缓存核心数据，支持历史查询 |
| `M2 可解释盘前选股` | 已能生成候选池、评分细项与交易计划 |
| `M3 可验证策略回测` | 已能输出两类策略的历史绩效与分布归因 |
| `M4 可监控实盘执行` | 已能在盘中监控候选、风险和卖点 |
| `M5 可审计完整闭环` | 已能在盘后输出复盘、违规与参数变更证据 |

### 5.6 开发完成定义

只有同时满足以下条件，首版才算“可用”而不是“能跑”：

1. 对任一候选标的，都能追溯 `为什么入池`、`为什么放弃`、`为什么成交`、`为什么卖出`。
2. 对任一交易日，都能追溯市场状态、盘中降档、仓位占用和违规事件。
3. 对任一策略参数调整，都能给出前后样本数、收益、回撤与失败样本证据。

## 6. 数据库与接口契约

### 6.1 设计原则

首版数据与接口设计以“可追溯、可回测、可审计”为优先，不以通用性为第一目标。原则如下：

1. 原始表、特征表、计划表、事件表、成交表、报表表分层存放。
2. 所有表必须保留 `source`、`ingested_at`、`trade_date` 等审计字段。
3. 所有接口都返回明确的结构化状态，禁止只靠文本描述判断成功失败。

### 6.2 建议的核心表结构

#### 6.2.1 `daily_bars`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `trade_date` | `date` | 交易日 |
| `symbol` | `string` | 股票代码 |
| `open` / `high` / `low` / `close` | `float` | OHLC |
| `volume` / `amount` | `float` | 成交量、成交额 |
| `pct_change` / `turnover_pct` / `amplitude_pct` | `float` | 核心日线衍生字段 |
| `source` | `string` | 数据来源 |
| `ingested_at` | `datetime` | 入库时间 |

主键建议：`trade_date + symbol`

#### 6.2.2 `minute_bars`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `bar_time` | `datetime` | 分钟时间 |
| `trade_date` | `date` | 所属交易日 |
| `symbol` | `string` | 股票代码 |
| `period` | `enum` | `1m` / `5m` / `15m` / `30m` / `60m` |
| `open` / `high` / `low` / `close` | `float` | OHLC |
| `volume` / `amount` | `float` | 成交量、成交额 |
| `vwap` | `float` | 均价 |
| `pct_change` / `turnover_pct` | `float` | 分钟衍生字段 |
| `source` / `ingested_at` | `string` / `datetime` | 审计字段 |

主键建议：`bar_time + symbol + period`

#### 6.2.3 `limit_up_events`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `trade_date` | `date` | 交易日 |
| `symbol` | `string` | 股票代码 |
| `board_count` | `int` | 连板数 |
| `first_limit_up_time` / `last_limit_up_time` | `string` | 首次/最后封板时间 |
| `board_break_count` | `int` | 炸板次数 |
| `seal_amount` | `float` | 封板资金 |
| `industry_name` | `string` | 所属行业 |
| `source` / `ingested_at` | `string` / `datetime` | 审计字段 |

主键建议：`trade_date + symbol`

#### 6.2.4 `limit_down_events`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `trade_date` | `date` | 交易日 |
| `symbol` | `string` | 股票代码 |
| `seal_amount` | `float` | 跌停封单资金 |
| `continuous_limit_down` | `int` | 连续跌停数 |
| `board_break_count` | `int` | 开板次数 |
| `source` / `ingested_at` | `string` / `datetime` | 审计字段 |

主键建议：`trade_date + symbol`

#### 6.2.5 `board_snapshots`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `snapshot_time` | `datetime` | 快照时间 |
| `trade_date` | `date` | 交易日 |
| `board_type` | `enum` | `industry` / `concept` |
| `board_code` / `board_name` | `string` | 板块标识 |
| `pct_change` | `float` | 板块涨跌幅 |
| `up_count` / `down_count` | `int` | 涨跌家数 |
| `leader_symbol` / `leader_pct_change` | `string` / `float` | 龙头与涨幅 |
| `source` / `ingested_at` | `string` / `datetime` | 审计字段 |

主键建议：`snapshot_time + board_type + board_code`

#### 6.2.6 `feature_snapshots`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `feature_id` | `string` | 特征快照主键 |
| `trade_date` | `date` | 交易日 |
| `symbol` | `string` | 股票代码 |
| `strategy_family` | `string` | 策略归属 |
| `market_regime` | `string` | 市场状态 |
| `feature_payload` | `json` | 特征明细 |
| `feature_version` | `string` | 特征版本号 |
| `created_at` | `datetime` | 生成时间 |

#### 6.2.7 `trade_plans`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `plan_id` | `string` | 计划主键 |
| `trade_date` | `date` | 交易日 |
| `symbol` | `string` | 股票代码 |
| `strategy_family` | `string` | 策略归属 |
| `candidate_rank` | `int` | 候选排名 |
| `planned_position_pct` | `float` | 计划仓位 |
| `planned_entry_branches` | `json` | 允许入场分支 |
| `entry_constraints` / `exit_constraints` | `json` | 阈值约束 |
| `status` | `string` | 计划状态 |
| `created_at` | `datetime` | 创建时间 |

#### 6.2.8 `signal_events`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `event_id` | `string` | 事件主键 |
| `plan_id` | `string` | 对应计划 |
| `symbol` | `string` | 股票代码 |
| `event_type` | `string` | 事件类型 |
| `decision` | `string` | 决策结果 |
| `event_payload` | `json` | 事件快照 |
| `event_time` | `datetime` | 事件时间 |
| `created_at` | `datetime` | 入库时间 |

#### 6.2.9 `execution_records`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `execution_id` | `string` | 成交主键 |
| `plan_id` | `string` | 来源计划 |
| `symbol` | `string` | 股票代码 |
| `entry_branch` / `exit_branch` | `string` | 买卖分支 |
| `entry_time` / `exit_time` | `datetime` | 买卖时间 |
| `entry_price` / `exit_price` | `float` | 买卖价格 |
| `position_pct` | `float` | 仓位 |
| `gross_return` / `net_return` | `float` | 收益 |
| `violation_flag` / `violation_reason` | `bool` / `string` | 违规信息 |
| `created_at` | `datetime` | 入库时间 |

#### 6.2.10 `review_reports`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `report_id` | `string` | 报表主键 |
| `trade_date` | `date` | 复盘日期 |
| `report_type` | `string` | `daily_review` / `parameter_change` / `backtest_summary` |
| `report_payload` | `json` | 报表内容 |
| `created_at` | `datetime` | 生成时间 |

### 6.3 表关系约束

1. `trade_plans.symbol + trade_date` 必须能关联到 `feature_snapshots`。
2. `signal_events.plan_id` 必须能关联到 `trade_plans.plan_id`。
3. `execution_records.plan_id` 必须能回溯到唯一计划。
4. `review_reports` 必须能关联到某个交易日或某次参数变更版本。

### 6.4 首版内部接口契约

#### 6.4.1 `DataIngestion` 接口

```json
{
  "request": {
    "dataset": "daily_bars|minute_bars|limit_up_events|limit_down_events|board_snapshots",
    "symbols": ["000001.SZ"],
    "trade_date": "2026-04-15",
    "start_time": "2026-04-15T09:15:00+08:00",
    "end_time": "2026-04-15T15:00:00+08:00",
    "force_refresh": false
  },
  "response": {
    "success": true,
    "dataset": "minute_bars",
    "rows": [],
    "source": "akshare",
    "cache_hit": true,
    "error": null
  }
}
```

#### 6.4.2 `StrategyEngine.generate_candidates`

```json
{
  "request": {
    "trade_date": "2026-04-16",
    "strategy_family": "first_board_to_second",
    "market_regime": "GREEN"
  },
  "response": {
    "success": true,
    "candidates": [
      {
        "symbol": "000001.SZ",
        "score_total": 24,
        "candidate_rank": 1,
        "hard_filter_pass": true,
        "rejection_reasons": []
      }
    ],
    "error": null
  }
}
```

#### 6.4.3 `PreMarketPlanner.create_trade_plan`

```json
{
  "request": {
    "trade_date": "2026-04-16",
    "candidate_ids": ["cand_001", "cand_002"]
  },
  "response": {
    "success": true,
    "plans": [
      {
        "plan_id": "plan_001",
        "symbol": "000001.SZ",
        "planned_position_pct": 0.4,
        "planned_entry_branches": ["first_surge_entry", "pullback_add"]
      }
    ],
    "error": null
  }
}
```

#### 6.4.4 `IntradayMonitor.evaluate_signal`

```json
{
  "request": {
    "plan_id": "plan_001",
    "quote_time": "2026-04-16T09:33:00+08:00",
    "snapshot": {}
  },
  "response": {
    "success": true,
    "event_type": "first_surge",
    "decision": "execute",
    "suggested_action": "buy",
    "suggested_position_pct": 0.4,
    "reasons": ["open_5m_return_ok", "vwap_support_ok"]
  }
}
```

#### 6.4.5 `BacktestEngine.run_backtest`

```json
{
  "request": {
    "strategy_family": "first_board_to_second",
    "start_date": "2024-01-01",
    "end_date": "2026-04-15",
    "parameter_version": "v1"
  },
  "response": {
    "success": true,
    "summary": {
      "trade_count": 0,
      "win_rate": 0.0,
      "profit_factor": 0.0,
      "max_drawdown": 0.0
    },
    "reports": ["review_001"],
    "error": null
  }
}
```

### 6.5 错误码与失败语义

首版内部接口统一约定以下错误语义，避免不同模块各说各话：

| 错误码 | 含义 | 处理建议 |
| :--- | :--- | :--- |
| `DATA_SOURCE_UNAVAILABLE` | 第三方接口不可用 | 重试、回退缓存、记录降级 |
| `DATA_INCOMPLETE` | 关键字段缺失 | 标记样本不可交易或不可回测 |
| `PLAN_CANCELLED_BY_RISK` | 风险降档导致计划失效 | 终止触发，写入事件日志 |
| `SIGNAL_NOT_CONFIRMED` | 信号条件未满足 | 保持观察，不成交 |
| `ORDER_UNFILLED` | 无法按规则成交 | 记录未成交原因 |
| `AUDIT_FAILED` | 交易记录或字段不完整 | 不计入有效样本 |

### 6.6 首版技术实现建议

本规范不强制具体技术栈，但建议遵守以下方向：

1. 数据存储优先选择支持结构化查询与 JSON 扩展的本地数据库。
2. 特征与回测计算优先做成可重复运行的批处理任务。
3. 盘中监控优先采用稳定的轮询架构，再考虑事件流架构。
4. 所有规则阈值尽量配置化，但配置读取后必须固化到版本记录中，避免“同名配置不同结果”。

## 7. 首版实施 Backlog

### 7.1 Backlog 组织方式

首版实施任务按 `Epic -> Feature -> Task` 三层管理：

1. `Epic` 对应一条完整业务能力，如“盘前选股”或“回测闭环”。
2. `Feature` 对应可独立验收的功能块，如“首板评分卡”“分钟线缓存”“卖出状态机”。
3. `Task` 对应可在 `1-3` 天内完成并交付代码/文档/测试结果的具体工作项。

所有任务必须带上以下元数据：

| 字段 | 说明 |
| :--- | :--- |
| `task_id` | 唯一任务编号 |
| `priority` | `P0` / `P1` / `P2` |
| `owner` | 负责人 |
| `dependency` | 前置依赖 |
| `deliverable` | 交付物 |
| `acceptance` | 验收标准 |
| `risk_note` | 风险备注 |

### 7.2 Epic 列表

| Epic ID | Epic 名称 | 目标 |
| :--- | :--- | :--- |
| `E1` | 数据底座 | 稳定接入并缓存策略所需原始数据 |
| `E2` | 特征与信号 | 把原始行情转成策略可直接消费的特征与状态机信号 |
| `E3` | 盘前执行 | 输出候选池、排序和交易计划 |
| `E4` | 回测验证 | 验证策略正期望、回撤和可执行性 |
| `E5` | 盘中监控 | 监控买点、卖点、风险降档和仓位约束 |
| `E6` | 盘后复盘 | 输出复盘、违规审计和参数迭代证据 |

### 7.3 P0 任务清单

`P0` 表示不完成就无法建立首版最小闭环的任务。

| Task ID | 所属 Epic | 任务 | 交付物 | 前置依赖 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `P0-001` | `E1` | 封装日线与分钟线拉取服务 | 数据接入模块 | 无 | 可按股票、时间范围稳定取数并落库 |
| `P0-002` | `E1` | 封装涨停池、跌停池、板块快照拉取服务 | 事件接入模块 | 无 | 可按交易日生成完整事件表 |
| `P0-003` | `E1` | 实现本地缓存与失败重试 | 缓存层、日志 | `P0-001`、`P0-002` | 第三方偶发失败时不会直接中断全流程 |
| `P0-004` | `E2` | 实现市场状态计算器 | `MarketRegime` 生成逻辑 | `P0-001`、`P0-002` | 可离线计算 `GREEN/YELLOW/RED` 与降档原因 |
| `P0-005` | `E2` | 实现一进二硬过滤与评分卡 | 候选生成逻辑 | `P0-001`、`P0-002` | 能输出候选、淘汰原因、评分明细 |
| `P0-006` | `E3` | 实现候选排序与同题材去重 | 候选排序器 | `P0-005` | 同板块保留主候选/备选，排名可解释 |
| `P0-007` | `E3` | 生成 `TradePlan` | 交易计划模块 | `P0-004`、`P0-006` | 每个计划包含仓位、允许分支、放弃条件 |
| `P0-008` | `E4` | 实现历史样本构建 | 样本生成模块 | `P0-001` 至 `P0-007` | 可生成 `MarketDaySample`、`BoardDaySample`、`TradeSample` |
| `P0-009` | `E4` | 实现事件驱动回测撮合 | 回测引擎 | `P0-008` | 支持买卖分支、滑点、未成交语义 |
| `P0-010` | `E4` | 输出核心指标与切片报告 | 回测报告 | `P0-009` | 覆盖第 `3.7.4` 节指标要求 |

### 7.4 P1 任务清单

`P1` 表示首版可用性和实盘辅助能力的重要补强任务。

| Task ID | 所属 Epic | 任务 | 交付物 | 前置依赖 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `P1-001` | `E2` | 实现 `2板弱转强` 特征与触发逻辑 | 辅助策略模块 | `P0-004` | 能独立生成弱转强候选与触发信号 |
| `P1-002` | `E5` | 实现竞价检查器 | 竞价监控模块 | `P0-007` | 可判断 `auction_reject` 与异常竞价 |
| `P1-003` | `E5` | 实现开盘 5 分钟承接监控 | 分钟信号模块 | `P0-007` | 可判定 `不瀑布杀`、`守住开盘价`、`first_surge_entry` |
| `P1-004` | `E5` | 实现统一卖出状态机 | 卖出引擎 | `P0-007` | 可执行 `P0-P5` 卖出优先级 |
| `P1-005` | `E5` | 实现风险降档广播 | 风控事件模块 | `P0-004` | 市场降档后可自动取消未触发计划 |
| `P1-006` | `E6` | 生成日度复盘报告 | `ReviewReport` | `P0-009`、`P1-004` | 自动输出当日候选、成交、违规、失败样本 |
| `P1-007` | `E6` | 实现违规审计 | 审计模块 | `P0-007`、`P1-004` | 可识别模式外交易、缺失字段、风控绕过 |

### 7.5 P2 任务清单

`P2` 表示体验优化和后续扩展任务，可在主闭环稳定后推进。

| Task ID | 所属 Epic | 任务 | 交付物 | 前置依赖 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `P2-001` | `E3` | 候选池可视化面板 | 简易 UI 或报表页 | `P0-007` | 可展示排名、评分和放弃原因 |
| `P2-002` | `E5` | 盘中监控面板 | 实时监控界面 | `P1-002` 至 `P1-005` | 可展示计划状态、风险事件和卖点提示 |
| `P2-003` | `E6` | 参数实验管理器 | 参数试验记录模块 | `P0-010` | 可记录版本、样本数、指标差异 |
| `P2-004` | `E1` | 多数据源回退机制 | 数据降级策略 | `P0-003` | 主源失败时可切换备用源或缓存 |

### 7.6 建议迭代节奏

#### Sprint 0：基础打底

目标：先让数据和规则“可重复跑通”。

1. 完成 `P0-001`、`P0-002`、`P0-003`。
2. 确认所有核心表可以稳定写入。
3. 用 `2024-01-01` 以来任意抽样周期做一次全链路数据落库演练。

#### Sprint 1：盘前与回测最小闭环

目标：先让系统能筛股、能计划、能回测。

1. 完成 `P0-004` 至 `P0-010`。
2. 对 `一进二打板` 做第一轮离线验证。
3. 输出第一版可复核的回测报告。

#### Sprint 2：盘中与盘后闭环

目标：让系统开始具备实盘辅助价值。

1. 完成 `P1-001` 至 `P1-007`。
2. 建立从盘前计划到盘后复盘的统一事件链。
3. 验证盘中降档、卖出优先级和违规审计是否生效。

#### Sprint 3：体验与扩展

目标：提升可用性和参数迭代效率。

1. 视情况推进 `P2` 任务。
2. 逐步增加面板、实验管理和多数据源降级。

### 7.7 Definition of Ready

任一任务进入开发前，至少满足以下条件：

1. 对应输入数据已在第 `6` 章定义清楚。
2. 输出实体或输出报表已在第 `4` 章定义清楚。
3. 验收标准明确到“如何判断完成”，而不是只有一句抽象目标。
4. 已知风险和前置依赖已写清。

### 7.8 Definition of Done

任一任务完成时，至少满足以下条件：

1. 代码或文档交付物已落地。
2. 关键路径已通过最小验证，包括成功样本和失败样本。
3. 日志、错误码和审计字段没有缺口。
4. 若任务改变了规则或阈值，已同步更新相应版本记录或参数说明。

### 7.9 建议的目录骨架

若首版采用 Python 为主实现，建议目录保持与本 Spec 对齐：

```text
MoneyAPP/
  docs/
    Spec.md
  data/
    raw/
    cache/
    warehouse/
    reports/
  src/
    ingestion/
    storage/
    features/
    strategies/
    backtest/
    monitor/
    review/
    common/
  configs/
    strategy/
    data_sources/
  scripts/
    sync_data.py
    run_backtest.py
    generate_plan.py
    review_day.py
  tests/
    unit/
    integration/
```

该目录只是建议，不是强制要求；但无论最终如何落地，模块边界都应尽量与第 `4`、`5`、`6` 章保持一致。
